# CURRICULUM v2 — JavaScript에서 React까지

이 문서는 현재 배포된 **10트랙 171레슨 교육과정의 원본**이다. 이전 9트랙 113레슨을 부분 수정하지 않고, JavaScript와 React를 처음 배우는 학습자가 이 플레이그라운드만으로 개념을 연결하도록 처음부터 다시 설계했다.

참고 기준은 《한 입 크기로 잘라 먹는 리액트》의 목차와 사용자가 제시한 “자료형 설명” 수준이다. 책의 Project 1 카운터, Project 2 할 일 관리, Project 3 감정 일기장 및 그 프로젝트에 종속된 구현은 제외한다. 설치·Vite·패키지처럼 현재 정적 플레이그라운드에서 실습할 수 없는 항목은 필수 과정에서 제외한다.

## 1. 최종 목표

학습자는 과정을 마친 뒤 다음을 설명하고 작성할 수 있어야 한다.

- JavaScript의 값, 자료형, 연산, 조건, 반복, 함수, 스코프를 설명한다.
- 원시값 복사와 객체 참조의 차이를 실행 결과로 예측한다.
- 객체와 배열의 원본 변경과 새 값 생성을 구분한다.
- 콜백, Promise, `async/await`의 실행 순서를 설명한다.
- DOM과 이벤트로 HTML에 동작을 붙인다.
- JSX, 컴포넌트, props, state, ref, effect의 역할을 구분한다.
- React가 상태 변경 뒤 다시 렌더링하는 과정을 설명한다.
- `useReducer`, Context, 메모이제이션, Web Storage를 필요한 상황에 사용한다.
- 코드를 보고 “왜 이렇게 작성했는가”를 자신의 말로 설명한다.

## 2. 학습자 전제와 설명 수준

- HTML과 CSS는 작성할 수 있지만 JavaScript 변수와 함수도 모른다고 가정한다.
- 영어 오류와 개발 용어를 처음 본다고 가정한다.
- 외부 강의나 검색 없이 다음 문제를 풀 수 있어야 한다.
- 앞 레슨을 끝냈어도 개념을 즉시 기억하지 못한다고 가정한다.

핵심 개념은 `정의 → 필요성 → 가장 작은 코드 → 실행 결과 → 대조 코드 → 흔한 실수 → 다음 연결` 순서로 설명한다. 예를 들어 자료형은 `typeof 42`에서 끝내지 않는다. `typeof null`, `typeof []`, `typeof NaN`, `Array.isArray`, 원시값 복사, 객체 참조를 알맞은 시점에 다루고 React의 불변 갱신으로 연결한다.

## 3. 레슨 계약

새 개념은 다음 순서를 거친 뒤에만 문제에서 요구한다.

| 단계 | 역할 | 새 문법 |
|---|---|---:|
| 읽기 | 실제 용어·필요·작동 원리를 코드와 설명 | 1개 |
| 실행 | 가장 작은 완성형의 입력과 결과 연결 | 0개 |
| 바꾸기 | 원인 한 곳을 바꾸고 결과 비교 | 0개 |
| 채우기 | 방금 본 모양의 한 자리 복원 | 0개 |
| 감싸기 | 이미 만든 식을 함수로 일반화 | 0개 |
| 쓰기 | 같은 개념을 작은 새 문제에 적용 | 0개 |

`write`에서 처음 보는 문법이나 API를 꺼내지 않는다. 종합 레슨은 새 개념을 가르치지 않는다. “앞에서 배웠습니다”라고만 쓰지 않고 당시 코드와 핵심 문장을 짧게 복원한다.

쉬운 설명은 실제 용어를 대체하지 않는다. 첫 등장에 `반환(return — 함수가 만든 값을 호출한 자리로 보내는 것)`처럼 병기하고 이후에는 일관되게 실제 용어를 쓴다.

## 4. 제외 범위

- Project 1 카운터 앱
- Project 2 할 일 관리 앱과 해당 앱 업그레이드
- Project 3 감정 일기장과 해당 앱 리팩터링·최적화·배포
- 위 프로젝트에 포함된 React Router 실습
- VS Code·Node.js·React Developer Tools 설치 절차
- npm 패키지 생성과 Vite 프로젝트 생성 절차

라우팅, 빌드 도구, 배포는 기본 과정 이후 후속 과정 후보로 남긴다.

---

# 새 트랙과 레슨

## T0. 실행과 값 — 21레슨

목표: 코드를 실행하고 값·변수·출력·오류를 구분한다.

| ID | 레슨 | 새 개념 |
|---|---|---|
| T0-01 | JavaScript는 어떻게 실행되는가 | 위에서 아래로 실행 |
| T0-02 | 값과 `console.log` | 값·출력 |
| T0-03 | 변수 선언 | `let`·`const`·할당 |
| T0-04 | 이름 짓기 | 식별자·명명 규칙 |
| T0-05 | 오류 읽기 | SyntaxError·ReferenceError·TypeError |
| T0-06 | 함수 맛보기 | 함수·호출·반환 |
| T0-07~21 | 종합 문제 1~15 | 값·변수·함수·매개변수·반환을 실무형 계산에 적용 |

`const`만 먼저 가르친 뒤 반복문에서 `let`을 갑자기 꺼내지 않는다. 두 선언의 차이는 여기서 함께 배운다.

## T1. 자료형과 연산 — 30레슨

목표: JavaScript 값의 종류와 연산 결과를 설명한다.

| ID | 레슨 | 새 개념·대조 |
|---|---|---|
| T1-01 | 자료형의 큰 지도 | 원시 타입과 객체 타입 |
| T1-02 | 문자열 | String·따옴표·`'42'`와 `42` |
| T1-03 | 숫자 | Number·NaN·Infinity·정수와 실수 |
| T1-04 | 불리언 | Boolean·비교 결과 |
| T1-05 | `undefined`와 `null` | 미할당과 의도적 없음·`typeof null` |
| T1-06 | Symbol과 BigInt | 고유값·큰 정수·기본 사용 범위 |
| T1-07 | 동적 타입과 `typeof` | 같은 변수에 다른 타입의 값 |
| T1-08 | 명시적 형변환 | `Number`·`String`·`Boolean` |
| T1-09 | 암시적 형변환 | `1 + '2'`와 `1 - '2'` |
| T1-10 | 산술·대입 연산자 | 계산·복합 대입 |
| T1-11 | 비교 연산자 | 초과/이상·값과 타입 |
| T1-12 | `==`와 `===` | 느슨한 동등과 엄격한 동등 |
| T1-13 | 논리 연산자 | `&&`·`||`·`!` |
| T1-14 | truthy와 falsy | `0`·빈 문자열·null·undefined·NaN |
| T1-15 | 단락 평가와 null 병합 | `&&`·`||`·`??`의 반환값 |
| T1-16~30 | 종합 문제 1~15 | 형변환·비교·논리 연산·기본값을 실무형 입력 처리에 적용 |

원시 타입 일곱 개를 숨기지 않되 Symbol과 BigInt의 문제 비중은 낮춘다. 면접 함정을 먼저 외우게 하지 않고 값의 성질을 실행한 뒤 정리한다.

## T2. 조건·반복·함수·스코프 — 17레슨

목표: 코드의 흐름을 나누고 반복되는 일을 함수로 묶는다.

| ID | 레슨 | 새 개념 |
|---|---|---|
| T2-01 | `if` | 조건문 |
| T2-02 | `else if`와 `else` | 여러 분기·경계값 |
| T2-03 | `switch` | 값별 분기 |
| T2-04 | 삼항 조건 연산자 | 값 하나를 고르는 조건식 |
| T2-05 | `for` 반복문 | 초기식·조건식·증감식 |
| T2-06 | `break`와 `continue` | 종료·건너뛰기 |
| T2-07 | 함수가 필요한 이유 | 중복 제거·이름 붙이기 |
| T2-08 | 함수 선언과 호출 | 선언 시점·실행 시점 |
| T2-09 | 매개변수와 인자 | parameter·argument |
| T2-10 | 반환 | `return`·출력과 반환 |
| T2-11 | 함수 표현식 | 함수를 값에 할당 |
| T2-12 | 콜백 함수 | 함수를 전달·누가 호출하는가 |
| T2-13 | 화살표 함수 | 축약 전후 대응 |
| T2-14 | 중첩 함수와 클로저 | 바깥 값 기억 |
| T2-15 | 스코프 | 전역·함수·블록 스코프 |
| T2-16 | 호이스팅 | 선언 처리 시점·TDZ 기초 |
| T2-17 | 종합 연습 | 조건·반복·continue·함수·반환 |

삼항 연산자를 `if`와 같은 레슨에 넣지 않는다. 콜백을 배운 뒤에만 배열 고차 메서드를 사용한다.

## T3. 객체·배열·참조 — 19레슨

목표: 여러 값을 구조화하고 원본 변경과 새 값 생성을 구분한다.

| ID | 레슨 | 새 개념 |
|---|---|---|
| T3-01 | 객체 만들기 | 객체·프로퍼티 |
| T3-02 | 프로퍼티 접근과 변경 | 점·대괄호 표기 |
| T3-03 | 메서드와 `this` 기초 | 객체의 함수·호출한 객체 |
| T3-04 | 배열 만들기 | 배열·인덱스·length |
| T3-05 | 원시값 복사 | 값 복사 |
| T3-06 | 객체 참조 | 같은 객체 공유 |
| T3-07 | 얕은 복사 | 전개 연산자·중첩 객체 |
| T3-08 | 구조 분해 할당 | 객체·배열 구조 분해·기본값 |
| T3-09 | rest와 spread | 모으기와 펼치기 |
| T3-10 | 배열 추가·삭제 | push·pop·shift·unshift |
| T3-11 | 배열 순회 | `forEach`·반환값 없음 |
| T3-12 | 배열 변형 | `map`·같은 길이의 새 배열 |
| T3-13 | 배열 탐색 | find·findIndex·includes |
| T3-14 | 배열 필터링 | `filter`·포함 여부 |
| T3-15 | 배열 합치기 | `reduce`·누산값·초깃값 |
| T3-16 | 정렬과 원본 변경 | `sort`·복사 후 정렬 |
| T3-17 | Date 객체 | 생성·조회·수정·출력 |
| T3-18 | ES 모듈 | export·import |
| T3-19 | 종합 연습 | 객체 배열·filter·map·reduce |

배열 메서드를 묶어서 암기시키지 않는다. 각 콜백이 무엇을 반환하고 원본이 변하는지를 별도 레슨에서 확인한다. 객체 참조는 뒤의 React 불변 갱신을 이해하기 위한 필수 선행 개념이다.

## T4. 브라우저 JavaScript와 비동기 — 17레슨

목표: HTML에 동작을 붙이고 시간이 걸리는 일을 처리한다.

| ID | 레슨 | 새 개념 |
|---|---|---|
| T4-01 | DOM과 요소 선택 | DOM·querySelector |
| T4-02 | 텍스트·클래스·속성 변경 | textContent·classList·attribute |
| T4-03 | 요소 생성과 추가 | createElement·append |
| T4-04 | 이벤트 리스너 | addEventListener |
| T4-05 | 이벤트 객체 | target·currentTarget |
| T4-06 | 이벤트 위임 | bubbling·closest |
| T4-07 | 입력과 변경 이벤트 | value·input·change |
| T4-08 | 폼 제출 | submit·preventDefault |
| T4-09 | 동기와 비동기 | 실행 순서·setTimeout |
| T4-10 | Promise의 상태 | pending·fulfilled·rejected |
| T4-11 | Promise 사용 | then·catch·finally |
| T4-12 | `async/await` | 비동기 함수·기다림 |
| T4-13 | 에러 처리 | throw·try/catch |
| T4-14 | fetch와 HTTP 응답 | fetch·ok·json |
| T4-15 | 여러 비동기 작업 | Promise.all·allSettled |
| T4-16 | Web Storage | localStorage·sessionStorage·JSON |
| T4-17 | 종합 연습 | DOM 탐색·이벤트·형변환·화면 갱신 |

Date와 Web Storage는 프로젝트 없이 독립 예제로 다룬다. Promise를 만드는 법과 사용하는 법을 한 레슨에 섞지 않는다.

## T5. React의 출발점 — 17레슨

목표: React가 무엇을 해결하고 컴포넌트가 어떻게 화면을 만드는지 설명한다.

| ID | 레슨 | 새 개념 |
|---|---|---|
| T5-01 | React가 필요한 이유 | 선언적 UI·직접 DOM 변경과 대조 |
| T5-02 | React 화면 생성 과정 | root·render |
| T5-03 | JSX는 값이다 | JSX 표현식 |
| T5-04 | JSX 문법 | 하나의 부모·Fragment |
| T5-05 | HTML과 다른 속성 | className·htmlFor |
| T5-06 | JavaScript 값 끼우기 | JSX의 `{}` |
| T5-07 | 조건부 렌더링 | 삼항·`&&` |
| T5-08 | JSX 스타일링 | className·style 객체 |
| T5-09 | 함수 컴포넌트 | 화면 조각을 함수로 만들기 |
| T5-10 | 컴포넌트 계층 | 부모·자식·분리 기준 |
| T5-11 | props | 부모에서 자식으로 값 전달 |
| T5-12 | 여러 종류의 props | 숫자·객체·함수·컴포넌트 |
| T5-13 | children | 자식 JSX 전달 |
| T5-14 | 이벤트 처리 | 호출과 함수 전달 |
| T5-15 | React 이벤트 객체 | SyntheticEvent·입력값 |
| T5-16 | 목록 렌더링 | map·key·안정적인 식별자 |
| T5-17 | 종합 연습 | 컴포넌트·props·조건부 JSX·목록 |

## T6. 상태와 입력 — 16레슨

목표: state가 바뀌면 React가 다시 렌더링한다는 정신 모델을 세운다.

| ID | 레슨 | 새 개념 |
|---|---|---|
| T6-01 | state가 필요한 이유 | 일반 변수와 화면 상태 |
| T6-02 | `useState` 기본 | 상태값·설정 함수 |
| T6-03 | 렌더와 스냅샷 | 렌더 시점의 값 |
| T6-04 | 함수형 업데이트 | 이전 상태 기반 갱신 |
| T6-05 | 훅 호출 규칙 | 최상위 호출·호출 순서 |
| T6-06 | 이벤트로 상태 변경 | 이벤트→설정→렌더 |
| T6-07 | 제어 컴포넌트 | value·onChange |
| T6-08 | 여러 입력값 | 객체 state·name |
| T6-09 | 객체 불변 갱신 | 새 객체·참조 변경 |
| T6-10 | 배열 불변 갱신 | map·filter·spread |
| T6-11 | props와 state | 받은 값과 소유한 값 |
| T6-12 | 상태 끌어올리기 | 공통 부모·단일 소유자 |
| T6-13 | 파생값 | 계산 가능한 값을 state로 두지 않기 |
| T6-14 | 조건별 UI 상태 | 빈 화면·오류·내용 |
| T6-15 | `useRef` | DOM ref·렌더와 무관한 값 |
| T6-16 | 종합 연습 | 제어 입력·배열 state·불변 갱신 |

불변 갱신을 “React 규칙”으로 외우게 하지 않고 T3의 객체 참조와 React의 참조 비교로 설명한다.

## T7. 라이프 사이클과 효과 — 12레슨

목표: 렌더링과 외부 시스템 동기화를 분리한다.

| ID | 레슨 | 새 개념 |
|---|---|---|
| T7-01 | 컴포넌트 라이프 사이클 | mount·update·unmount |
| T7-02 | effect가 필요한 이유 | 외부 시스템 동기화 |
| T7-03 | `useEffect` 기본 | 렌더 뒤 실행 |
| T7-04 | 의존성 배열 | 없음·빈 배열·값 배열 |
| T7-05 | 여러 의존성 | 사용한 값과 의존 관계 |
| T7-06 | 정리 함수 | cleanup |
| T7-07 | 마운트와 언마운트 | 구독 시작·해제 |
| T7-08 | 필요 없는 effect | 렌더 중 계산과 effect 구분 |
| T7-09 | effect에서 fetch | 로딩·성공·실패 |
| T7-10 | 경쟁 조건 | 늦게 온 응답 무시 |
| T7-11 | 커스텀 훅 | state와 effect 묶음 재사용 |
| T7-12 | 종합 연습 | 커스텀 훅·구독·정리 함수 |

## T8. 복잡한 상태와 데이터 공급 — 11레슨

목표: 상태 전환이 복잡하거나 props 전달이 깊을 때 사용할 도구를 고른다.

| ID | 레슨 | 새 개념 |
|---|---|---|
| T8-01 | 상태 변경 코드의 복잡성 | 흩어진 전환 로직 |
| T8-02 | reducer 함수 | state·action·다음 상태 |
| T8-03 | `useReducer` | dispatch |
| T8-04 | action 설계 | type·payload |
| T8-05 | `useState`와 `useReducer` | 선택 기준 |
| T8-06 | props drilling | 깊은 전달 문제 |
| T8-07 | Context | Provider·소비자 |
| T8-08 | Context와 state | 값과 변경 함수 공급 |
| T8-09 | Context 분리 | 관심사와 렌더 범위 |
| T8-10 | reducer와 Context 조합 | 조합 기준과 남용 방지 |
| T8-11 | 종합 연습 | reducer·Provider·Context·dispatch |

PDF의 할 일 관리 앱을 재사용하지 않는다. 각 개념은 장바구니 수량, 테마, 필터처럼 서로 독립된 작은 예제로 확인한다.

## T9. 렌더링 최적화와 저장 — 11레슨

목표: 측정 없이 최적화하지 않고 참조와 렌더링의 관계를 이해한다.

| ID | 레슨 | 새 개념 |
|---|---|---|
| T9-01 | 다시 렌더링되는 이유 | 부모 렌더·state·Context |
| T9-02 | 최적화 전 측정 | 느린 계산 관찰 |
| T9-03 | `useMemo` | 계산 결과 메모이제이션 |
| T9-04 | `React.memo` | props 비교 |
| T9-05 | 함수 참조 | 렌더마다 만들어지는 함수 |
| T9-06 | `useCallback` | 함수 메모이제이션 |
| T9-07 | 최적화의 비용 | 의존성·복잡성·사용하지 않을 때 |
| T9-08 | localStorage에 저장 | JSON 직렬화 |
| T9-09 | 처음 상태 복원 | `useState` 초기화 함수 |
| T9-10 | 저장과 effect | 상태 변경과 저장 동기화 |
| T9-11 | 종합 연습 | 초기화 함수·useMemo·effect·저장 |

## 5. 전체 규모

| 트랙 | 레슨 |
|---|---:|
| T0 실행과 값 | 21 |
| T1 자료형과 연산 | 30 |
| T2 조건·반복·함수·스코프 | 17 |
| T3 객체·배열·참조 | 19 |
| T4 브라우저 JavaScript와 비동기 | 17 |
| T5 React의 출발점 | 17 |
| T6 상태와 입력 | 16 |
| T7 라이프 사이클과 효과 | 12 |
| T8 복잡한 상태와 데이터 공급 | 11 |
| T9 렌더링 최적화와 저장 | 11 |
| **합계** | **171** |

133개 개념 레슨을 기반으로 각 트랙 끝에 종합 문제를 확장하고 있다. 현재 T0와 T1은 종합 문제 15개 구성을 마쳤고, T2~T9도 같은 방식으로 확장할 예정이다. 종합 문제는 새 개념을 추가하지 않고 해당 트랙에서 배운 내용을 실무형 상황에 조합한다.

## 6. 반복할 설명 질문

각 핵심 단원의 마지막에는 코드 작성과 함께 다음을 묻는다.

- 이 값의 자료형은 무엇이며 왜 그런가?
- 출력과 반환은 어떻게 다른가?
- 이 함수는 누가 언제 호출하는가?
- 이 코드는 원본을 바꾸는가, 새 값을 만드는가?
- 두 변수는 값을 따로 갖는가, 같은 객체를 참조하는가?
- 이 상태를 어느 컴포넌트가 소유해야 하는가?
- 이 코드는 렌더 중 계산인가, 렌더 뒤 effect인가?
- 이 최적화는 어떤 실제 작업을 줄이는가?

## 7. 작성 금지 사항

- “앞에서 배웠습니다”만 쓰고 개념을 복원하지 않는다.
- 설명하지 않은 문법을 보조 코드에 넣고 무시하길 기대하지 않는다.
- 실제 용어를 검색할 수 없는 지어낸 말로 교체하지 않는다.
- 한 문제에서 새 API와 새 문제 해결 전략을 동시에 요구하지 않는다.
- 정답 코드가 실행된다는 사실을 학습 가능성의 증거로 쓰지 않는다.
- 면접 함정을 본 개념보다 먼저 나열하지 않는다.
- React 훅을 사용법 목록으로 외우게 하지 않는다.

## 8. 레슨 완료 기준

- 선행 개념은 이전 레슨에서 정의·실행·변경·검사를 모두 거쳤다.
- 새 용어는 첫 등장에 실제 용어와 쉬운 뜻이 함께 있다.
- `fill`과 `write`에 처음 등장하는 문법이 없다.
- 문제 전에 같은 코드 구조의 완성형을 실행했다.
- solution은 모두 통과하고 starter는 모두 실패한다.
- 학습 목표와 검사 목표가 하나로 일치한다.
- 결과를 자신의 말로 설명할 질문이 있다.

## 9. 전환 결과

2026년 8월 24일에 T0~T9의 133개 레슨을 `lessons/`로 일괄 전환했다. 저장 키를 `js-playground:v2`로 바꾸고 revision을 3으로 올려 기존 과정의 진행 기록과 분리했다. 정적 구조 검사 133개와 실제 브라우저 실행 검사 197건을 통과한 뒤 전환했다.
