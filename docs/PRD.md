# PRD — 웹퍼블리셔를 위한 JS/React 학습 플레이그라운드

| 항목 | 내용 |
|---|---|
| 문서 버전 | v0.1 (초안) |
| 작성일 | 2026-08-18 |
| 작성자 | 신호진 |
| 프로젝트 코드명 | (미정 — 가칭 `Bridge`) |
| 배포 형태 | 정적 사이트 / GitHub Pages |
| 사용자 규모 | N = 1 (본인) |

---

## 0. 전제 하나

이 문서는 "학습용 도구"를 정의한다. 그런데 이 도구를 만드는 과정 자체가 이 도구로 배우려는 내용(DOM, 이벤트, 상태 관리, 모듈 설계)의 실전판이다. 도구 완성이 목표가 아니라 **도구를 만들면서 T1~T2를 통과하고, 완성된 도구로 T3~T4를 통과하는 것**이 실제 시나리오다. 스코프 판단은 전부 이 기준으로 한다.

---

## 1. 문제 정의

### 1.1 현재 상황

13년 커머스 경력 후 웹퍼블리싱으로 전환. HTML/CSS는 대규모 프로젝트 2건(392p, 164p)으로 검증된 상태. 그러나 JS/React는 다음 지점에서 막힌다.

| 구분 | 현재 | 목표 |
|---|---|---|
| HTML/CSS | 대규모 실전 경험 | 유지 |
| JS | 코드를 읽을 수는 있음 | 백지에서 작성 |
| React | 개념만 인지 | 마크업을 컴포넌트로 분해 |

### 1.2 기존 학습 수단의 한계

| 수단 | 한계 |
|---|---|
| 강의/책 | 예제가 남의 코드다. 내 마크업과 연결되지 않는다 |
| CodePen·CodeSandbox | 자유도는 높지만 커리큘럼이 없다. 뭘 해야 할지가 없다 |
| 온라인 코딩 문제 | 알고리즘 중심. 퍼블리셔가 실무에서 쓰는 DOM/이벤트/상태와 무관 |
| 프로젝트 클론 | 난이도 점프가 크다. 중간 단계가 비어 있다 |

**핵심 결핍: "내가 이미 만든 마크업"을 교재로 쓰는 학습 경로가 없다.**

### 1.3 제품 가설

정적 사이트 한 페이지 안에서 (1) 문제 제시 (2) 코드 작성 (3) 즉시 실행 (4) 자동 검증이 한 화면에 붙어 있고, 교재가 **JW Anderson·HOOT UP의 실제 마크업**이면, 퍼블리싱 → JS → React 전환의 심리적·기술적 간극이 줄어든다.

---

## 2. 사용자

### 2.1 유일 페르소나

- 43세, 웹퍼블리셔 전환 중, 구직 활동 병행
- CSS 아키텍처(토큰, `:is()` 압축, 시맨틱 알리아스)에는 강함 → **추상화·구조화 사고는 이미 있다**
- 접근성·문서화에 대한 기준이 높음 → 도구 자체도 그 기준을 만족해야 사용을 지속함
- 학습 시간은 구직 활동 사이의 파편 시간 → **한 세션 15~25분 안에 레슨 1개가 끝나야 함**

### 2.2 사용 맥락

| 맥락 | 요구 |
|---|---|
| 노트북, 카페 | 오프라인/저속 회선에서도 초기 로드가 버티는 용량 |
| 짧은 세션 | 이어하기 — 진행률과 마지막 코드가 보존 |
| 구직 중 | 학습 흔적이 곧 근거가 되도록 진행 기록이 남음 |

---

## 3. 제품 정의

### 3.1 한 줄 정의

> 마크업을 이미 쓸 줄 아는 사람이, 그 마크업을 재료로 JS와 React를 손으로 익히는 정적 웹 플레이그라운드.

### 3.2 설계 원칙

1. **한 화면에서 끝난다** — 탭 이동, 페이지 이동 없음
2. **교재는 내 코드다** — 예제 마크업은 실제 포트폴리오에서 발췌
3. **틀린 걸 도구가 알려준다** — 눈으로 비교하지 않고 검증이 판정
4. **레슨은 데이터다** — 레슨 추가에 애플리케이션 코드 수정이 필요 없어야 한다
5. **도구가 학습을 방해하지 않는다** — 무한루프, 로딩 지연, 상태 소실은 학습 중단 사유다

### 3.3 명시적 비범위 (Out of Scope)

| 제외 항목 | 사유 |
|---|---|
| 서버 / DB / 로그인 | 정적 배포 전제. 사용자 1명 |
| npm 패키지 임의 설치 | 브라우저 번들러 필요. 비용 대비 학습 효과 낮음 |
| TypeScript | JS를 먼저 확보. v2 이후 |
| 다중 파일 프로젝트 | MVP는 단일 파일. 파일 분리는 T4 종료 후 별도 판단 |
| 공유 링크 / 협업 | 사용자 1명. v2에서 URL 인코딩으로 검토 |
| 자동 채점 점수·랭킹 | 게임화 불필요. 통과/미통과만 |
| 모바일 코드 작성 | 뷰어(읽기)까지만 지원. 편집은 데스크톱 |

---

## 4. 학습 트랙 구조

트랙 4개 × 레슨 8개 = **총 32레슨 고정**. 이 숫자는 스코프 방어선이다. 늘리지 않는다.

### T1 — JS 기초 문법 (8레슨)

| # | 주제 | 검증 방식 |
|---|---|---|
| 1 | 변수·타입·형변환 | 반환값 assert |
| 2 | 함수·화살표 함수·스코프 | 반환값 assert |
| 3 | 배열 메서드 (map/filter/reduce) | 반환값 assert |
| 4 | 객체·구조 분해·전개 연산자 | 반환값 assert |
| 5 | 조건·반복 리팩터링 | 반환값 assert |
| 6 | 템플릿 리터럴로 HTML 문자열 생성 | 문자열 정규화 비교 |
| 7 | 모듈 패턴 (import/export) | 반환값 assert |
| 8 | 비동기 기초 (Promise / async·await / fetch) | 비동기 assert |

*설계 메모: 6번은 T2·T3의 다리 역할. 마크업을 데이터로부터 만드는 첫 경험.*

### T2 — DOM · 이벤트 인터랙션 (8레슨)

| # | 주제 | 실습 소재 |
|---|---|---|
| 1 | 선택·순회 (querySelector / closest / dataset) | 카드 리스트 |
| 2 | 클래스·속성 조작 | 탭 UI |
| 3 | 이벤트 바인딩과 이벤트 객체 | 버튼 그룹 |
| 4 | 이벤트 위임 | 상품 리스트 (JW Anderson 발췌) |
| 5 | 폼 입력 제어 + 수량 조절 | `qtyComponent` 재구현 |
| 6 | 조건부 렌더링 / 빈 상태 처리 | 검색 필터 |
| 7 | 접근성 있는 인터랙션 (aria-expanded, 포커스 관리) | 아코디언 |
| 8 | 종합: 필터 + 정렬 + 카운트 | 코스 카드 그리드 (HOOT UP 발췌) |

### T3 — 퍼블리싱 → React 컴포넌트 전환 (8레슨) ★ 핵심 트랙

이 트랙이 이 제품의 존재 이유다. 좌측 패널에 **실제 프로젝트 마크업**이 주어지고, 우측에서 그것을 컴포넌트로 옮긴다.

| # | 주제 | 소재 |
|---|---|---|
| 1 | JSX 문법과 HTML의 차이 (class→className, self-closing) | 버튼 |
| 2 | 첫 컴포넌트 — 마크업 그대로 감싸기 | 상품 카드 |
| 3 | props로 하드코딩 값 뽑아내기 | 상품 카드 → `<ProductCard>` |
| 4 | 리스트 렌더링과 key | 상품 그리드 |
| 5 | children과 합성 (composition) | 섹션 래퍼 |
| 6 | 조건부 렌더링 (품절/뱃지/할인) | 상품 카드 변형 |
| 7 | 컴포넌트 분해 기준 — 어디서 자를 것인가 | 코스 카드 (HOOT UP) |
| 8 | CSS 연결 전략 (전역 클래스 유지 vs CSS Modules 개념) | 종합 |

*설계 메모: 7번은 정답이 하나가 아니다. 검증 대신 "참고 답안 비교" 모드로 처리한다.*

### T4 — React 상태 · 훅 (8레슨)

| # | 주제 |
|---|---|
| 1 | `useState` — 값이 바뀌면 화면이 바뀐다 |
| 2 | 이벤트 핸들러와 상태 업데이트 |
| 3 | 배열·객체 상태의 불변 업데이트 |
| 4 | 폼과 제어 컴포넌트 |
| 5 | 상태 끌어올리기 (lifting state up) |
| 6 | `useEffect` — 언제 필요하고 언제 불필요한가 |
| 7 | 커스텀 훅으로 로직 분리 |
| 8 | 종합: 장바구니 (T2-5, T3-3 재활용) |

---

## 5. 기능 요구사항

### 5.1 MVP (P0)

| ID | 기능 | 요구 내용 |
|---|---|---|
| F-01 | 3분할 레이아웃 | 좌: 문제/설명 · 중: 에디터 · 우: 미리보기+콘솔. 패널 리사이즈 가능 |
| F-02 | 코드 에디터 | 구문 강조, 자동 들여쓰기, 괄호 매칭, 탭 키 처리. CodeMirror 6 (ESM CDN) |
| F-03 | 실행 엔진 | `sandbox` 속성 iframe + `srcdoc` 주입. 실행마다 iframe 재생성으로 상태 초기화 |
| F-04 | 콘솔 미러 | iframe 내부 `console.*` 후킹 → `postMessage` → 우측 콘솔 패널. 객체는 펼침 가능 |
| F-05 | 에러 표시 | 구문 에러 / 런타임 에러를 줄 번호와 함께 표시. `window.onerror` + `unhandledrejection` 포착 |
| F-06 | JSX 트랜스파일 | T3/T4 진입 시에만 트랜스파일러 lazy load. React/ReactDOM은 UMD CDN |
| F-07 | 자동 검증 | 레슨별 assert 목록 실행 → 통과/실패 개별 표시. 실패 시 기대값과 실제값 병기 |
| F-08 | 레슨 데이터 | `/lessons/*.json` 정적 파일. 앱 코드 수정 없이 레슨 추가 가능 |
| F-09 | 진행률 저장 | `localStorage`. 트랙별 완료 수, 레슨별 최종 코드, 마지막 위치 |
| F-10 | 이어하기 | 재방문 시 마지막 레슨 + 마지막 코드 복원 |
| F-11 | 리셋 / 정답 보기 | 초기 코드 복원, 참고 답안 토글 (통과 후 또는 3회 실패 후 노출) |
| F-12 | 무한루프 방어 | 실행 후 N초 내 완료 신호 없으면 iframe 강제 종료 + 안내 |

### 5.2 P1 (MVP 이후)

| ID | 기능 |
|---|---|
| F-13 | 다크/라이트 테마 |
| F-14 | 진행률 대시보드 (트랙별 완료율, 학습일 기록) |
| F-15 | 코드 포맷팅 (Prettier standalone) |
| F-16 | 자유 실습 모드 (레슨 없는 빈 플레이그라운드) |
| F-17 | 진행 데이터 JSON export / import |

### 5.3 P2 (검토만)

URL 압축 인코딩 공유 · 다중 파일 · TypeScript · 커스텀 레슨 작성 UI

---

## 6. 기술 설계

### 6.1 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 앱 | Vanilla JS (ES Modules) | 빌드 없음. 학습 대상을 학습 도구에 쓰지 않는다 |
| 에디터 | CodeMirror 6 | ESM CDN 임포트 |
| 트랜스파일 | Sucrase 또는 @babel/standalone | §6.3 참조 |
| React | React 18 + ReactDOM UMD | iframe 내부에서만 로드 |
| 스타일 | CSS 커스텀 프로퍼티 2단 토큰 (primitive → semantic) | 기존 프로젝트 규약 승계 |
| 저장 | `localStorage` | 서버 없음 |
| 배포 | GitHub Pages | `hotshin24` 계정 |

### 6.2 실행 파이프라인

```
[에디터 코드]
      │
      ├─ T1/T2 ──────────────────────────┐
      │                                   │
      └─ T3/T4 → 트랜스파일(JSX→JS) ─────┤
                                          ▼
                        [srcdoc 문자열 조립]
                        · console 후킹 스크립트
                        · 에러 핸들러
                        · 실행 완료 핑
                        · (T3/T4) React UMD
                        · 사용자 코드
                        · assert 실행 코드
                                          ▼
                        [sandbox iframe 재생성]
                                          │
                              postMessage │
                                          ▼
                        [부모: 콘솔 / 검증 결과 렌더]
```

**iframe sandbox 속성:** `allow-scripts` 만 부여. `allow-same-origin` 미부여 → 부모 DOM·스토리지 접근 차단.

### 6.3 트랜스파일러 선택 (결정 필요)

| 후보 | 대략 용량 | 장점 | 단점 |
|---|---|---|---|
| Sucrase | 작음 | 빠름, JSX 변환에 충분 | 구형 문법 다운레벨 미지원 |
| @babel/standalone | 큼 | 사실상 표준, 에러 메시지 친절 | 초기 로드 부담 |
| esbuild-wasm | 중간(wasm) | 매우 빠름, 번들링 가능 | wasm 초기화 복잡도 |

**잠정 결정: Sucrase.** 최신 브라우저만 지원하면 되므로 다운레벨이 불필요하고, T3 진입 시 로딩 대기가 학습 흐름을 끊지 않는 것이 우선. 단, 구문 에러 메시지 품질을 M2에서 실측하고 미흡하면 Babel로 교체한다. — *이 판단은 M2 스파이크 결과로 확정.*

### 6.4 레슨 데이터 스키마 (초안)

```json
{
  "id": "t3-03",
  "track": "T3",
  "order": 3,
  "title": "props로 하드코딩 값 뽑아내기",
  "estimatedMin": 20,
  "brief": "마크업 설명 (마크다운)",
  "reference": {
    "label": "JW Anderson 상품 카드",
    "html": "<article class=\"pcard\">…</article>",
    "css": ".pcard{…}"
  },
  "starterCode": "function ProductCard() {\n  return null;\n}",
  "solutionCode": "…",
  "hints": ["…", "…"],
  "runtime": "react",
  "asserts": [
    {
      "label": "제목이 props로 전달된다",
      "type": "dom",
      "selector": ".pcard__title",
      "expectText": "…"
    }
  ]
}
```

**assert 타입:** `value`(반환값) · `console`(출력) · `dom`(선택자+텍스트/속성) · `html`(정규화 비교) · `manual`(참고 답안 비교만)

### 6.5 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 초기 로드 | T1/T2 진입 시 트랜스파일러·React 미로드 |
| 실행 지연 | T1/T2 300ms 이내, T3/T4 첫 실행 1.5s 이내 (이후 캐시) |
| 무한루프 타임아웃 | 3초 |
| 접근성 | Lighthouse Accessibility 100. 에디터 키보드 탈출 경로 보장(Esc → Tab) |
| 반응형 | ≥1280px 3분할 / 768~1279px 2분할+토글 / <768px 읽기 전용 |
| 브라우저 | 최신 Chrome·Safari·Edge |
| 코드 규약 | `!important` 0건, 하드코딩 색상 0건, Conventional Commits |

---

## 7. 성공 지표

사용자가 1명이므로 트래픽 지표는 의미가 없다. **학습 완주**와 **도구 품질** 두 축으로만 측정한다.

### 7.1 학습 지표

| 지표 | 목표 |
|---|---|
| 32레슨 완주율 | 100% |
| 트랙별 완주 시점 | T1·T2 각 2주, T3 3주, T4 3주 |
| 정답 보기 사용 비율 | T4 기준 30% 이하 (= 스스로 푼 비율 70%) |
| 최종 산출물 | 플레이그라운드 없이 React로 상품 카드 그리드를 백지에서 구현 |

### 7.2 도구 지표

| 지표 | 목표 |
|---|---|
| Lighthouse Accessibility / Best Practices / SEO | 각 100 |
| 신규 레슨 추가 비용 | JSON 1건 추가, 앱 코드 수정 0줄 |
| 실행 실패(도구 버그로 인한) 발생 | 32레슨 통과 과정 중 0건 |
| 문서화 | PRD · 기능정의서 · README 3종 완비 |

---

## 8. 마일스톤

| 단계 | 산출물 | 완료 조건 |
|---|---|---|
| **M0. 스파이크** | 실행 엔진 프로토타입 | textarea 코드 → iframe 실행 → `console.log`가 부모 패널에 뜬다. 무한루프가 3초에 끊긴다 |
| **M1. T1/T2 가동** | 에디터 + 검증 + 레슨 로더 | T1·T2 16레슨이 데이터만으로 동작 |
| **M2. 트랜스파일** | JSX 파이프라인 | 트랜스파일러 확정, React 렌더 성공, DOM assert 동작 |
| **M3. T3/T4** | 전체 32레슨 | 진행률 저장·이어하기·정답 보기 완비 |
| **M4. 마감** | 접근성·반응형·문서 | Lighthouse 목표 달성, 문서 3종, GitHub Pages 배포 |

**M0가 이 프로젝트의 성패를 가른다.** M0가 안 되면 나머지는 전부 UI 작업일 뿐이고, M0가 되면 나머지는 시간 문제다. M0에 막히면 스코프를 줄이지 말고 **트랙을 줄여라** (T1·T2만으로 v1 출시).

---

## 9. 리스크

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| R1 | 레슨 콘텐츠 제작이 도구 개발보다 오래 걸림 | 높음 | 레슨은 트랙당 8개 고정. 도구 완성 후 레슨을 채운다. 순서를 바꾸지 않는다 |
| R2 | 트랜스파일러 로딩이 학습 흐름을 끊음 | 중 | lazy load + 첫 로드 후 캐시. T3 진입 시 1회만 |
| R3 | 무한루프로 브라우저 정지 | 중 | 타임아웃 강제 종료(F-12). M0에서 먼저 검증 |
| R4 | `localStorage` 초기화로 진행 소실 | 중 | JSON export 기능(F-17)을 P1 최상단으로. 주 1회 수동 백업 |
| R5 | 도구 만들기에 몰입해 정작 학습을 안 함 | **높음** | M1 완료 시점에 T1 8레슨을 먼저 푼다. 개발과 학습을 교대 진행 |
| R6 | 구직 활동과 시간 경합 | 높음 | 면접·지원이 우선. 이 프로젝트는 후순위로 명시. 중단 시 M 단위로 끊어 재개 |
| R7 | 스코프 팽창 (TS, 다중 파일, 공유 기능) | 중 | §3.3 비범위 목록을 변경 시 문서 개정 절차를 거친다 |

---

## 10. 열린 질문

1. 프로젝트명 확정 (`Bridge` / `퍼블리셔의 React` / 기타)
2. 트랜스파일러 최종 선택 — M2 스파이크 후 결정
3. T3-7(컴포넌트 분해 기준)처럼 정답이 여러 개인 레슨의 검증 방식 — `manual` 타입으로 충분한가
4. 레퍼런스 마크업 발췌 범위 — 원본 프로젝트 CSS를 그대로 쓸 것인가, 축약본을 별도 관리할 것인가
5. 향후 포트폴리오 3번째 작품으로 전환할 경우 추가로 필요한 것 (공개 데모 시나리오, 성능 수치 문서화)

---

## 부록 A. 이 문서의 개정 규칙

- §3.3(비범위)과 §4(32레슨 고정)를 변경할 때는 버전을 올리고 변경 사유를 기록한다.
- §6.3, §10의 미결 항목은 결정 즉시 본문에 반영하고 열린 질문에서 삭제한다.
- 측정값(성공 지표, 성능)은 **실측치만** 기재한다. 추정치는 기재하지 않는다.
