{
  "schemaVersion": 2, "id": "t8-02", "track": "T8", "order": 2, "runtime": "js",
  "title": "reducer 함수로 다음 state 계산하기",
  "steps": [
    {
      "kind": "read", "title": "현재 state와 action을 받아 다음 state를 반환합니다",
      "brief": [
        "reducer는 현재 state와 action을 매개변수로 받고 다음 state를 반환하는 순수 함수입니다.",
        { "code": ["function reducer(state, action) {", "  if (action.type === 'increase') return state + 1;", "  return state;", "}"] },
        "action은 무슨 일이 일어났는지를 나타내는 객체입니다. type 프로퍼티로 변경 종류를 구분합니다.",
        "reducer 안에서는 기존 state를 직접 변경하거나 fetch 같은 외부 작업을 하지 않습니다. 같은 state와 action에는 같은 다음 state를 반환해야 합니다.",
        "모르는 action에서는 현재 state를 그대로 반환하거나 오류를 던지는 정책을 정할 수 있습니다. 이 과정에서는 현재 state를 반환합니다."
      ]
    },
    {
      "kind": "run", "title": "React 없이 상태 전환 확인하기",
      "brief": ["quantityReducer는 일반 JavaScript 함수이므로 직접 호출해 결과를 확인할 수 있습니다.", "0에서 increase는 1, 다시 increase는 2, decrease는 1, reset은 0을 반환합니다."],
      "code": "function quantityReducer(state, action) {\n  if (action.type === 'increase') return state + 1;\n  if (action.type === 'decrease') return Math.max(0, state - 1);\n  if (action.type === 'reset') return 0;\n  return state;\n}\n\nlet state = 0;\nstate = quantityReducer(state, { type: 'increase' });\nstate = quantityReducer(state, { type: 'increase' });\nstate = quantityReducer(state, { type: 'decrease' });\nconsole.log(state);\nconsole.log(quantityReducer(state, { type: 'reset' }));\n"
    },
    {
      "kind": "tweak", "title": "새 action 규칙 추가하기",
      "brief": ["action.type이 'set-five'이면 5를 반환하는 조건을 추가하고 직접 호출해 보세요.", "reducer는 state를 저장하지 않습니다. 전달받은 현재 state로 다음 값을 계산해 반환할 뿐입니다.", "다음 레슨에서 useReducer가 현재 state를 보관하고 action을 reducer에 전달합니다."],
      "code": "function quantityReducer(state, action) {\n  if (action.type === 'increase') return state + 1;\n  if (action.type === 'decrease') return Math.max(0, state - 1);\n  if (action.type === 'reset') return 0;\n  return state;\n}\n\nlet state = 0;\nstate = quantityReducer(state, { type: 'increase' });\nstate = quantityReducer(state, { type: 'increase' });\nstate = quantityReducer(state, { type: 'decrease' });\nconsole.log(state);\nconsole.log(quantityReducer(state, { type: 'reset' }));\n"
    }
  ]
}
